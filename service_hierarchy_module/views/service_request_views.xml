<odoo>
<!-- TREE VIEW -->
    <record id="view_service_request_tree"
            model="ir.ui.view">

        <field name="name">service.request.tree</field>

        <field name="model">service.request</field>

        <field name="arch" type="xml">

            <tree>

                <field name="name"/>
                <field name="phone"/>
                <field name="service_id"/>
                <field name="new_service"/>
                <field name="subservice_id"/>
                <field name="new_subservice"/>
                <field name="state"/>

            </tree>

        </field>

    </record>

    <!-- FORM VIEW -->
    <record id="view_service_request_form"
            model="ir.ui.view">

        <field name="name">service.request.form</field>

        <field name="model">service.request</field>

        <field name="arch" type="xml">

            <form>

                <header>

                    <button name="action_approve"
                            type="object"
                            string="Approve"
                            class="btn-primary"
                            groups="service_hierarchy_module.group_service_admin"
                            invisible="state != 'pending'"/>

                    <button name="action_reject"
                            type="object"
                            string="Reject"
                            class="btn-danger"
                            groups="service_hierarchy_module.group_service_admin"
                            invisible="state != 'pending'"/>

                    <field name="state"
                           widget="statusbar"/>

                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="executive_id"/>
                            <field name="service_id"/>
                            <field name="new_service"/>
                            <field name="subservice_id"/>
                            <field name="new_subservice"/>
                        </group>
                        <group>
                            <field name="name"/>
                            <field name="phone"/>
                            <!-- <field name="discount"/> -->
                            <field name="owner_id"/>
                        </group>
                    </group>
                    <group>
                        <field name="address"/>
                    </group>
                    <notebook>
                        <page string="Gallery">
                            <field name="gallery_ids">
                                <tree editable="bottom">
                                    <field name="image"
                                        widget="image"/>
                                </tree>
                            </field>
                        </page>
                        <page string="Facilities">
                            <field name="facility_ids">
                                <tree editable="bottom">
                                    <field name="name"/>
                                </tree>
                            </field>
                        </page>
                        <page string="Discounts">
                            <field name="discount_ids">
                                <tree editable="bottom">
                                    <field name="name"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- ACTION -->
    <record id="action_service_request"
            model="ir.actions.act_window">
        <field name="name">Service Requests</field>
        <field name="res_model">service.request</field>
        <field name="view_mode">tree,form</field>
    </record>

</odoo>